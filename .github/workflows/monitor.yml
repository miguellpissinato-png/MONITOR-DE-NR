name: Monitor Diário — SST e DOU

on:
  # O cron do GitHub é BEST-EFFORT: atrasa e às vezes simplesmente não roda.
  # Ele fica aqui só como rede de segurança. O disparo confiável deve vir de
  # um agendador externo via repository_dispatch (ver README).
  schedule:
    - cron: '0 9 * * 1-5'
    - cron: '0 11 * * 1-5'
    - cron: '0 13 * * 1-5'
  repository_dispatch:
    types: [monitor]
  workflow_dispatch:
    inputs:
      reason:
        description: 'Motivo da execução manual'
        required: false
        default: 'Verificação manual'
      dry_run:
        description: 'Só verificar, sem gravar o estado no repositório'
        required: false
        type: boolean
        default: false

permissions:
  contents: write

# Duas execuções simultâneas disputariam o push do state.json. Serializa.
concurrency:
  group: monitor-sst
  cancel-in-progress: false

jobs:
  monitor-sst:
    runs-on: ubuntu-latest
    # Pior caso (toda fonte esgotando as 3 tentativas com backoff): 69s por
    # fetch x 14 fetches = 16,1 min por passada, x2 passadas + 45s = 33 min.
    # Execução normal leva ~1 min; a folga existe só para o dia em que o gov.br
    # estiver fora do ar. Se o job for MORTO pelo timeout, o passo de gravação
    # não roda e o painel congela com dado velho — por isso a margem.
    # AO ACRESCENTAR FONTE, REFAÇA ESTA CONTA.
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 1

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependências do relatório
        if: ${{ !inputs.dry_run }}
        run: pip install --quiet anthropic reportlab

      # TEMPORÁRIO — amostra do cadastro do MMA. Remover depois.
      - name: Amostra MMA
        run: python scripts/_amostra_mma.py

      - name: Executar monitoramento
        id: monitor
        run: |
          set -uo pipefail
          # O script agora repete cada URL 3x com backoff, então esta camada
          # existe só para o caso de uma indisponibilidade mais longa. Duas
          # passadas bastam: três estouravam o limite de tempo do job.
          for tentativa in 1 2; do
            echo "--- Passada $tentativa de 2 ---"
            if python scripts/check_nr.py; then
              exit 0
            fi
            [ "$tentativa" -lt 2 ] && echo "Aguardando 45s..." && sleep 45
          done
          echo "::error::As 2 passadas falharam — ao menos uma fonte crítica está inacessível."
          exit 1

      # O relatório é uma camada extra: se falhar, não derruba o monitoramento,
      # que é a função essencial. Sem o secret ANTHROPIC_API_KEY o script apenas
      # avisa e sai com sucesso.
      - name: Gerar relatório de análise
        if: success() && !inputs.dry_run
        continue-on-error: true
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python scripts/gerar_relatorio.py

      # Roda mesmo se o passo acima falhou: o state.json já contém o registro
      # da falha, e é ele que faz o painel exibir "DESATUALIZADO".
      - name: Salvar estado
        if: always() && !inputs.dry_run
        run: |
          set -euo pipefail
          git config user.name  "Monitor SST Bot"
          git config user.email "monitor-sst@github-actions"
          git add data/state.json relatorios/ 2>/dev/null || git add data/state.json
          if git diff --cached --quiet; then
            echo "Nada mudou no estado."
            exit 0
          fi
          git commit -m "chore: monitoramento SST $(date -u +'%Y-%m-%d %H:%M UTC')"
          for tentativa in 1 2 3; do
            git pull --rebase --autostash origin "${GITHUB_REF_NAME}" || true
            if git push origin "HEAD:${GITHUB_REF_NAME}"; then
              exit 0
            fi
            echo "Push falhou (tentativa $tentativa) — nova tentativa em 10s..."
            sleep 10
          done
          echo "::error::Não foi possível gravar o state.json."
          exit 1
